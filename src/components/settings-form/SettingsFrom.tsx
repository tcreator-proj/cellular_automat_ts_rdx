import React from 'react'
import { connect } from 'react-redux'

export const SettingsFrom = (props: any) => {
  return (
    <div>SettingsFrom</div>
  )
}

const mapStateToProps = (state: any) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(SettingsFrom)